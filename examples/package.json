{
  "name": "examples",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/index.html ./dist && npx esbuild ./src/index.ts --outfile=dist/index.js --format=esm --bundle --platform=browser && npx esbuild ./src/worker.ts --outfile=dist/worker.js --format=esm --bundle --platform=browser"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "esbuild": "^0.25.11"
  }
}
